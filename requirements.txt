# Pinned dependency versions for FL-IDS. Update deliberately, not by
# leaving versions floating. Verified compatible with Python 3.14 / aarch64
# (manylinux) wheels as of 2026-09-23 — see Phase 0 setup notes.

# Core numerics / data
numpy==2.5.3
pandas==3.0.6
scikit-learn==1.9.1
pyyaml==6.0.3

# Boosting classifier (component 2)
lightgbm==4.7.0

# Autoencoder (component 3)
# CPU-only build — this machine has no NVIDIA GPU, and the default PyPI
# wheel drags in several GB of unused CUDA dependencies.
# Install with: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu

# Federated learning orchestration (components 4, 7, 8)
flwr==1.38.0

# SDN mitigation bridge (component 9)
flask==3.1.3
requests==2.34.2

# Raw traffic handling for Phase B (.pcap replay support tooling)
scapy==2.7.0

# Dashboard (component 13)
streamlit==1.64.0

# Evaluation / plotting (component 12)
matplotlib==3.11.2
joblib==1.6.0

# Testing
pytest==8.4.2
